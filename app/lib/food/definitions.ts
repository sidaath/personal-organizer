export type InventoryItemType = {
    id: string,
    itemName: string,
    size: number,
    units: string,
    quantity: number,
    expiry: string | null | undefined
}

export type ToBuyItemType = {
    id: string,
    itemName: string,
    size: number,
    units: string,
    quantity: number,
}

